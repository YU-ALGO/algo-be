package com.stock.yu.downbitbe.board.domain.post;

import org.springframework.data.jpa.repository.JpaRepository;

public interface PostImageTempRepository extends JpaRepository<PostImageTemp, Long> {
}
